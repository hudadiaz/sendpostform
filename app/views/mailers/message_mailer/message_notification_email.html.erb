<p><strong>You received a message at</strong>: <%= @message.created_at.strftime('%I:%M %d %B %Y') %></p>
<table>
  <thead>
    <tr>
      <th style="width: 1%">Item</th>
      <th style="text-align: left">Input</th>
    </tr>
  </thead>
  <tbody>
    <% (eval @message.content).each do |item| %>
      <tr>
        <td><code><%= item.first %></code></td>
        <td><%= item.second %></td>
      </tr>
    <% end %>
  </tbody>
</table>